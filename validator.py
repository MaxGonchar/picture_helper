# get next unsorted image
# generate URL
# provide URL, is_good, likelihood to user
# get user feedback
# in transaction:
    # store image to sorted
    # update sorted tags
    # remove image from unsorted
# provide next image if user wants to continue or image exists
